import * as types from '../constants/ActionTypes'
import Immutable from 'immutable'


const initialState = [];


export default function example(state = initialState, action) {

  switch (action.type) {
    /*case types.RECEIVE_EXPERIENCE_DATA:
      return action.data;
      break;*/
    default:
      return state
  }
}
